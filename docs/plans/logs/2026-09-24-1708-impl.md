review session: a7f9b551-2719-48c6-8372-2632262d1a15

## 1回目

````text
ビルド・テストの実行はこのセッションでは許可されなかったため（`mise run test` が承認待ちで止まった）、以下は静的レビューのみです。

## P0
- `scripts/preflight.sh:TEAM_ID` — Team ID が `project.yml` と食い違っている（scripts 側は `VYDUR99LAM` が 3 箇所、`project.yml:DEVELOPMENT_TEAM` は `U4WAHGGBR7`） / `CODE_SIGN_STYLE: Manual` ＋ xcconfig で証明書ハッシュ直指定なので、証明書がある実機で Release を焼くと「選択した Team と署名 ID が一致しない」で落ちるか、preflight が検査したのとは別 Team の bundle が出来る。`mise run signing` が解決する証明書と `DEVELOPMENT_TEAM` の対が崩れているので、Phase 5 の初回リリースはこのままでは通らない / `security find-identity -v -p codesigning` で Developer ID Application の実際の Team を確認し、`project.yml`・`preflight.sh`・`gen-signing-xcconfig.sh`・`make-release-zip.sh` の 4 箇所を片方に揃える（keyrc 由来の `VYDUR99LAM` が正なら `project.yml` を直す）

## P1
- `.mise.toml:tasks.install` — Release を `build-release` → `install-app.sh Release` で直接置いており、`make-release-zip.sh` の inside-out 再署名を通らない / 埋め込み Sparkle.framework の内部（XPCServices・Updater.app・Autoupdate）は xcodebuild が ad-hoc のまま残すので、外側だけ Developer ID という配布物とは違う署名状態の常用版が `/Applications/mycast.app` に入る。Phase 5 の「旧バージョンを置いてから更新が通ることを確認する」をこのタスクでやると、配布物では起きない失敗（または逆に通ってしまう）で判断を誤る / `install` を `make-release-zip.sh` 経由にして再署名後の .app を置くか、タスクの説明に「配布物とは署名状態が違う。更新経路の確認には使わない」と明記して用途を切る
- `Sources/Launcher/LauncherController:focusField` — `LauncherTextField.current` が nil なら黙って return し、次ループの再試行も `panel.isKeyWindow` と `currentEditor()` しか見ない（field 自体は再解決しない） / SearchField の `makeNSView` は NSHostingView が最初にレイアウトされるまで走らないため、初回 `show()` でフィールド生成が間に合わないと検索欄にフォーカスが乗らないまま開き、しかも回復経路が無い（閉じて開き直すしかない）。フォーカスは合成イベントで検証できず VERIFY.md でも人間確認に回している箇所なので、失敗しても気づきにくい / 再試行ブロックの中で `LauncherTextField.current` を取り直す。併せて `show()` の activate 分岐の前に `hosting?.layoutSubtreeIfNeeded()` を呼んでフィールド生成を確定させる
- `scripts/gen-emoji.py:main` — `k` に日本語のローマ字を入れていない（CLDR の en / ja の keywords と ja の tts をそのまま連結するだけ） / `Sources/Core/EmojiSearch.swift:EmojiEntry.k` のコメントと plan の「検索キーワード（英語・日本語・日本語のローマ字）」に反する。絵文字モードは開くたびに ABC へ切り替わる仕様なので、`warau`・`iine` では引けず毎回かなキーを押す必要があり、ローマ字キーワードはその前提を埋めるためのものだった / `gen-emoji.py` で `j` と日本語 keywords を `AppPathRules.romanized` 相当（`CFStringTransform` の kCFStringTransformToLatin）でローマ字化して `k` に足し `mise run emoji` で再生成する。データを増やさない判断なら `EmojiEntry.k` のコメントと plan の記述を実装に合わせて直す

## P2
- `Sources/Clipboard/ClipboardMonitor:markOwnWrite` — `ownChangeCounts` に入れた値は取り出されない（同時に `lastChangeCount` も更新するので `poll()` が `remove` に到達する前に return する） / 自分の書き込みのスキップは `lastChangeCount` だけで足りており、Set は 1 回のコピーごとに 1 要素増え続ける死んだ経路 / `ownChangeCounts` を消すか、`lastChangeCount` の更新をやめて Set 側だけで判定するかに一本化する
- `Sources/Launcher/LauncherController:show` — `activate: true` の経路で `savedInputSource` を無条件に上書きする / 既にパネルが見えている状態でもう一度呼ぶと ABC を「元の入力ソース」として保存してしまい、閉じても日本語に戻らない。現状は `toggleLauncher` / `toggleEmoji` が `isShown` で弾き、メニューの `openLauncher` もステータス項目が key を奪って先に閉じるため踏まないが、ガードはどこにも無い / `guard savedInputSource == nil` を入れるか、`openLauncher` を `toggleLauncher` 相当にする
- `Sources/Launcher/AppIndex:saveCache` — `NSMetadataQueryDidUpdate` のたびに `DELETE` ＋全件 `INSERT` をメインスレッドで回す / Homebrew の一括更新などでアプリが次々書き換わると、1000 件規模の書き直しが繰り返しメインスレッドを塞ぐ。パネルが開いている最中だと入力が引っかかる / 差分だけ書く、もしくは更新を数秒デバウンスしてからまとめて保存する
- `Sources/Launcher/Paster:finishPaste` — `appActive == false` の縮退でトーストを出すだけで、mycast がアクティブなまま（パネルは閉じているのでウィンドウは無い）残る / キーフォーカスがどこにも無い状態になり、次の打鍵が捨てられる / この分岐で `NSApp.hide(nil)` を呼んでアクティブを手放す
- `Sources/Clipboard/ClipboardStore:ClipItem.fileURLs` — ファイル参照を改行区切りの 1 カラムに詰めている / macOS のファイル名には改行を含められるため、該当ファイルをコピーすると復元時にパスが分割されて別物になる / JSON 配列で持つか、区切りを `\0` にする
- `Sources/Core/FuzzyMatcher:score` — 「語をつなげた形の一致も見る（"sys set" → "system settings"）」とコメントしているが、実装は各トークンを独立に候補全体へ当てているだけで連結形は見ていない（例の挙動はトークン単位の一致で偶然成立している。`sysset` は一致しない） / 実装と読み合わせたときに誤解を招く / コメントを実装に合わせるか、連結クエリの一致を実装する
- `Sources/Launcher/LauncherController:layoutPanel` — コメントと実装が「上 1/5」、plan は「上 1/3 あたり」 / 実機で見て決める類だが、根拠が 2 つあると次に触るときに迷う / どちらかに寄せて plan かコメントを直す
- `Sources/App/Log:write` — ローテーションも上限も無く追記し続ける / 常駐アプリで `panel.shown` / `clipboard.recorded` を毎回書くので、数か月で無視できない大きさになる / 起動時にサイズを見て一定以上なら 1 世代だけリネームする
- `scripts/make-release-zip.sh` — 末尾で `mise run release:notarize` と `mise run publish-release` を案内しているが、`.mise.toml` にそのタスクは無い（あるのは `release` と `release:preflight`） / 手動で分割実行しようとしたときに存在しないコマンドを叩くことになる / 案内文を実在するタスク名に直す（`scripts/notarize.sh` を直接叩く旨にする）

## Q
- `Sources/Launcher/AppIndex:collectSettingsPanes` — システム設定パネルを `x-apple.systempreferences:<appex の bundle id>` で開く実装だが、plan の「Phase 2 の最初に実機で全 50 件開けるかを確かめ、開けないものだけ固定表で補う」は「ユーザーの画面に 50 回システム設定が開く」ため人間側に回され、VERIFY.md の人間確認は「数件」の抜き取りに縮んでいる。全件を一度通すのか、抜き取りのままにして開かないパネルが見つかった時点で固定表を足す運用にするのかが未決 / 抜き取りのまま進めると、普段使わないパネルだけが無言で開かない状態が残り、原因が URL 生成規則なのか個別 appex の都合なのか切り分けられないまま常用に入る

````

**対応**: P0（Team ID の食い違い）は意図どおりのため変更なし: 開発用 DEVELOPMENT_TEAM=U4WAHGGBR7（個人 Team）と配布用 Developer ID の Team=VYDUR99LAM は別物で、personal-mac-apps.md に「絞り込みは VYDUR99LAM、project.yml の値をコピーすると落ちる」と明記、keyrc も同じ組み合わせでリリース実績あり。再発防止に project.yml へコメントを追加。P1: install タスクの説明に「再署名・notarize を通らないので更新経路の確認には使わない」を明記／focusField は再試行で LauncherTextField.current を取り直し、show で hosting.layoutSubtreeIfNeeded() を呼んでから activate、取れなければログ／絵文字のローマ字は入れない（絵文字画面は IME を許す方針に変えたため）→ EmojiEntry.k のコメントを実装に合わせた。P2: ownChangeCounts の死んだ Set を削除、show の savedInputSource を nil のときだけ保存、finishPaste の app_not_active で NSApp.hide、FuzzyMatcher のコメント修正、パネル位置は plan を「上 1/5（上端固定）」に更新、make-release-zip の案内を実在の手順に修正。見送り（追加系）: saveCache のデバウンス、ログのローテーション、ファイルパスの改行対策（改行を含むファイル名は実用上まれで、保存形式の変更になる）。Q（設定パネル全件確認）→ 全件は確認しない。appex 自身が allowsXAppleSystemPreferencesURLScheme を宣言したものだけ載せており、抜き取りで開かないものが出たら固定表で補う（plan の方針変更に記録）。

## 2回目

````text
前回の対応を確認しました。`build/xcodebuild-debug.log` / `xcodebuild-test.log`（17:05）はいずれも `** BUILD SUCCEEDED` / `** TEST SUCCEEDED` です。P0 の Team ID は、Manual 署名＋`CODE_SIGN_IDENTITY` 直指定では `DEVELOPMENT_TEAM` はプロファイル選択にしか効かないため、説明どおり取り違えではないことを確認しました（コメントも入っています）。以下は今回の差分で新しく生じた点だけです。

## P0

## P1
- `Sources/Launcher/Paster.swift:finishPaste`（plan `Phase 3 > 4`） — `app_not_active` の分岐で `NSApp.hide(nil)` を呼んだ直後に `Toast.show()` している / `hide` はアプリを hidden 状態にし、以降にオーダーインされたウィンドウは unhide まで表示が持ち越される（`Toast` のパネルは `canHide` が既定の true）。`orderFrontRegardless()` は「アプリが非アクティブでも前に出す」だけで hidden は越えない。結果、3 つある縮退分岐のうち唯一ユーザーが原因を推測しにくい「貼り付け先を前面にできなかった」ケースでだけトーストが出ず、ログを見ないと黙ってコピーだけされたように見える / アクティブを手放す目的なら `NSApp.hide(nil)` ではなく `NSApp.deactivate()` を使う（ウィンドウは既に `close(.paste)` で orderOut 済みなので hidden にする必要がない）。`hide` を維持するなら `Toast` のパネル生成時に `p.canHide = false` を足す

## P2
- `docs/plans/2026-09-24-1624-mycast初期実装.md:前提 > ルート検索`（plan `Phase 2 > 4`） — 方針変更に「システム設定パネルの全件を URL で開く確認はしない」を追記した一方、前提の該当行は「appex の識別子がそのまま URL に使えるとは限らないので、Phase 2 の最初に実機で全件開けるかを確かめ、開けないものだけ固定表で補う」のまま残っている / 同じ文書の中で前提と方針変更が反対のことを言っており、次に plan を読む側は前提（＝仕様の記述）を先に信じる。過去の指摘対応でも「本文とチェックリストの両方に入れる」を基準にしてきた箇所 / 前提の行を「`allowsXAppleSystemPreferencesURLScheme` を宣言した appex だけを載せる。全件確認はせず、抜き取りで開かないものが出たら固定表で補う」に書き換える（`ログ > 試したこと` の 50 件確認の記述も現状に合わせる）
- `Sources/Launcher/LauncherController.swift:focusField` — 新設した `panel.focus_failed no_field` が VERIFY.md のイベント名一覧（`panel.shown` / `panel.closed reason=…` など grep 用に固定した語彙）に入っていない / この保険は「フォーカスが乗らなかった」を後から追うために足したもので、一覧に無いと人間確認で不調が出たときに探す語が分からず、保険が機能しない / VERIFY.md の「ログは `~/Library/Logs/mycast/…`」の列挙に `panel.focus_failed` を足す

## Q

````

**対応**: P0 なしで収束。P1: finishPaste の app_not_active 分岐を NSApp.hide から NSApp.deactivate に変更（hide だと後に出すトーストも隠れる）。P2: plan の前提（ルート検索）の設定パネル確認の行と、ログの 50 件確認の記述を方針変更に合わせて書き換え／VERIFY.md のイベント名一覧に panel.focus_failed を追加。
