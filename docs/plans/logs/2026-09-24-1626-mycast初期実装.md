review session: 43a3c047-f253-41e4-8514-cda85236e460

## 1回目

````text
`~/keyrc` と references はこのセッションの作業ディレクトリ外で読めなかったので、keyrc からの移植部分（scripts / .mise.toml / 署名まわりの実体）は plan の記述を前提に、macOS 側の実装制約を中心にレビューしました。

## P0
- `UI・常駐 > 2` — 「非アクティブ化しない `.nonactivatingPanel`」と「閉じたら元のアプリにフォーカスを戻す」「元のアプリを前面に戻してから ⌘V」が両立しない。本当に非アクティブ化しないならフォーカスは奪っていないので復帰処理は no-op、逆にテキスト入力を安定して受けるには key window 化（＝実質アクティブ化）が要る / どちらのモデルかでキーイベントの取り方・前面アプリの記憶・パネル外クリック検出・貼り付け手順が全部変わり、Phase 2 と Phase 3 を作り直すことになる / Raycast と同じ「⌃L で `NSWorkspace.shared.frontmostApplication` を記憶 → `NSApp.activate()` → `canBecomeKey = true` の NSPanel を key にする → 閉じるときに記憶したアプリを `activate()`」に一本化し、plan の文言も揃える。
- `Raycast から引き継ぐ固定の挙動 > 3` — 入力ソース ABC 切り替えに、閉じたときの復元が書かれていない。`TISSelectInputSource` はシステム全体の入力ソースを変える / 日本語入力の最中に ⌃L を押して Esc で閉じただけで、元アプリが ABC に戻らないまま残る。毎日踏む壊れ方で、しかも「履歴から貼り付け」の直後にも起きる / 表示前に `TISCopyCurrentKeyboardInputSource` を保存して閉じるときに戻す。切り替えは即時に効かず最初の 1 打が旧ソースに入ることがあるので、パネルの入力欄側でも IME を無効化する。`動作確認 > 3` に「閉じた後に元アプリが日本語入力に戻っていること」を足す。
- `Phase 2 > 5` — 起動回数の保存先が決まっていない。SQLite が登場するのは Phase 3 / Phase 2 を UserDefaults で書くと Phase 3 で DB に寄せ直す移行コードが丸ごと無駄になる。最近使った絵文字（Phase 4）も同じ問題 / Phase 1 に「Application Support のパス解決 ＋ SQLite（system libsqlite3 の薄いラッパで十分、SPM 依存を足さない）」を入れ、起動回数・履歴・最近使った絵文字を同じ DB に置く。
- `Phase 3 > 4` — dev 版のアクセシビリティ許可が開発ループで維持できない。TCC は bundle id と署名（designated requirement）で紐づくので、アドホック署名で毎回ビルドし直すと cdhash が変わるたびに許可が外れる / Phase 3 の検証（⌘V 合成）が毎回「許可し直し」になり、縮退パスばかり踏むことになる / dev も常用と同じ Developer ID で署名し（keyrc の `gen-signing-xcconfig.sh` の対象を Debug にも広げる）、`/Applications/mycast Dev.app` の固定パスに install して使う運用を Phase 1 の `mise run install` に含める。

## P1
- `Phase 2 > 4` — 対象が 4 ディレクトリの直下のみ。`/Applications/Utilities`、ベンダーのサブフォルダ、`~/Applications/Chrome Apps.localized` の PWA、Xcode 同梱アプリを取りこぼす / 「あるはずのアプリが出ない」は launcher として致命的に体感が悪い / `.app` の中には降りない 1〜2 階層の再帰にするか、`NSMetadataQuery`（`kMDItemContentType == "com.apple.application-bundle"`）で集める。後者なら FSEvents も不要になる。
- `Phase 2 > 5` — 「起動回数」で並べると、一度だけ大量に開いたアプリが上位に居座り続ける / Raycast の体感（frecency）と差が出て、Phase 5 前の試用で必ず違和感として返ってくる / スキーマに `last_used_at` を持たせ、回数に時間減衰をかける。スキーマを後から足すのは面倒なので Phase 1 の DB 設計時に入れておく。
- `Phase 2 > 7` — `--snapshot <png>` の実装手段が未定。macOS 14 以降は `CGWindowListCreateImage` も ScreenCaptureKit も、自分のウィンドウを撮るだけで画面収録許可を要求する / 自動検証ループが TCC ダイアログで止まり、検証フックの意味が無くなる / `NSBitmapImageRep(forCachingDisplayIn:)` か `CALayer.render(in:)` でプロセス内描画して PNG 化する（許可不要）。
- `Phase 1 > 1` — 最小 macOS バージョンが書かれていない。`SMAppService`（13+）、`/System/Library/ExtensionKit/Extensions` 配下の設定パネル（13+）、SwiftUI の API 差に効く / project.yml は Phase 1 の成果物なので、後で下げると全部ビルドし直し・API 分岐の追加になる / 実機は macOS 26 系なので `deploymentTarget: macOS "26.0"` を明記する（下げるなら 13.0 が下限）。
- `Phase 1` — テストターゲットが無い。あいまい一致のスコアリング、エイリアス完全一致の 1 位固定、履歴の重複判定は純粋関数で、仕様がぶれやすい箇所 / 後から project.yml にテストターゲットとスキームを足すのは Phase 1 のやり直しになる / Phase 1 に `mycastTests` を作り、Phase 2 / 3 の各項目にテストを書く前提にする。
- `クリップボード履歴 > 3` — 自分が貼り付けのために書いたペーストボードを、次のポーリングで新規コピーとして拾う / 画像・ファイルだと同じ実体をもう一度保存してディスクを食う。さらに dev と常用を並行させている間は二重記録になる / 書いた直後の `changeCount` を覚えて 1 回スキップする。除外型に `org.nspasteboard.AutoGeneratedType` も足す。
- `クリップボード履歴 > 7` — ⌘V 合成の手順が「前面化 → 書く → 合成」だけ。`activate()` は非同期で、直後に CGEvent を投げると前のアプリか誰にも届かない。ユーザーが押しっぱなしの修飾キー（⌘Enter の ⌘ など）も混ざる / 「たまに貼り付かない」という再現しづらいバグになる / 前面化の完了（`NSRunningApplication.isActive` の監視、目安 50–100ms）を待ってから、`CGEventSource(stateID: .privateState)` で `.maskCommand` を明示して投げる。
- `絵文字ピッカー > 2` — CLDR 由来の生成物を public repo にコミットするのにライセンス表記が無い / Unicode License はコピーに著作権表示を要求する / Phase 4 に `LICENSE-THIRD-PARTY`（Unicode License と CLDR のバージョン）を置く項目を足す。
- `UI・常駐 > 2` — パネルの `level` と `collectionBehavior` が未指定 / 既定のままだとフルスクリーンの Chrome や Spaces をまたいだ場面でパネルが出ない・前に来ない / `.floating` 以上 ＋ `.canJoinAllSpaces` ＋ `.fullScreenAuxiliary` を明記する。
- `動作確認 > 2` — 検証項目に ⌘Enter（コピーのみ）とアクセシビリティ未許可時の縮退表示が無い / Phase 3 の 2 つの分岐のうち片方しか人間が確認しないまま常用に入る / VERIFY.md に「許可を切った状態で Enter → コピーに落ちて、その旨が出る」を足す。

## P2
- `ルート検索 > 3` — エイリアスをコードに焼くと、1 つ足すだけで release が要る / `~/Library/Application Support/mycast/aliases.json` があれば読み、無ければ組み込み既定、にしておくと設定画面を作らない方針のまま運用で回せる。
- `クリップボード履歴 > 2` — 保持が期間のみで件数・容量の上限が無い。画像込み 3 か月は GB 級になりうる / 件数上限か合計サイズ上限を併用し、行を消すときに画像ファイルの孤児も掃除する。
- `Phase 5 > 6` — dev を常駐させたまま常用版を入れると、ポーリングと DB が二重に走る / Phase 5 の手順に「dev を終了・ログイン項目から外す」を入れる。
- `UI・常駐 > 5` / `Raycast から引き継ぐ固定の挙動 > 2` — macOS 26 では既定の見た目が変わっているので、「ダークの半透明」を自前で塗るより `NSVisualEffectView(.hudWindow)` に寄せた方が OS 更新に強い。
- `Raycast との並行期間 > 2` — ホットキー登録失敗の通知は、LSUIElement + 起動直後という条件では `UNUserNotificationCenter` が出ないことがある / 起動時の失敗は NSAlert、以後はメニューバーアイコンの状態表示で気づけるようにする方が確実。

## Q
- `クリップボード履歴 > 8` — 履歴（テキスト・画像・ファイル）を平文の SQLite で 3 か月持つことを許容するか。Concealed/Transient で除外できるのはパスワードマネージャ経由だけで、手でコピーしたトークンや業務の文面は素のまま残る / 決めないと Phase 3 のスキーマ（暗号化の有無・鍵を Keychain に置くか）を後から変えることになる。
- `絵文字ピッカー > 2` — 肌の色バリエーション（👍🏽 等）を候補に出すか、既定のトーンを 1 つ持つか、素のコードポイントだけにするか / 決めないと Phase 4 のデータ生成（JSON の件数が数倍変わる）とグリッドの見せ方を作り直すことになる。

````

**対応**: P0 4件すべて反映（フォーカスモデルを「前面アプリ記憶→自アプリをアクティブ化→key な NSPanel→閉じたら記憶したアプリへ戻す」に一本化／入力ソースは閉じたら元に戻す＋入力欄で IME 無効化、動作確認に復元を追記／SQLite ラッパを Phase 1 に移し起動回数・最近使った絵文字も同じ DB に／dev も安定証明書で署名し `/Applications/mycast Dev.app` 固定パスで運用）。P1 のうち書き換えで済むものを反映: アプリ収集を NSMetadataQuery に、並べ替えを frecency に、スナップショットはプロセス内描画、deploymentTarget macOS 26.0、自分の書き込みの changeCount をスキップ＋AutoGeneratedType 除外、⌘V は前面化完了待ち＋独立イベントソース、パネルの level/collectionBehavior。P2 のうち書き換えのもの: NSVisualEffectView、ホットキー失敗は NSAlert＋メニューバー表示。見送り（追加系のため終了報告へ）: テストターゲット追加、CLDR のライセンス表記、動作確認への ⌘Enter／許可なし縮退の項目、履歴の件数・容量上限、Phase 5 の dev 終了手順。aliases.json は「固定でよい」というユーザー方針に反するので見送り。
Q: 履歴の暗号化 → しない（今回の範囲外。根拠: FileVault 前提の個人機・Raycast も端末内に保持していた）。肌の色 → 基本コードポイントのみ（範囲外。根拠: 既定トーン設定の話はヒアリングに一切出ていない）。いずれも plan 本体に明記。

## 2回目

````text
再読しました。前回の P0 4 件と反映対象の P1/P2 は、本文・チェックリストの両方に入っていることを確認しました。見送り分は繰り返しません。以下は今回の書き換えで新しく生じた／依然残っている点です。

## P0
- `Phase 2 > 2` — 「Esc・パネル外クリックで閉じ、記憶した元のアプリをアクティブ化して戻す」が、閉じる理由で場合分けされていない。パネル外クリックは「別アプリのウィンドウをクリックした」場合を含む / ユーザーが Safari をクリックして閉じたのに、記憶していた Chrome を前面に戻してしまい、クリックしたアプリがすぐ裏に回る。フォーカスモデルを一本化した直接の帰結なので、このまま書くと必ず実装され、試用で必ず直すことになる / 復帰は「Esc / ホットキー再押下 / 項目の実行（アプリ起動・貼り付け）」のときだけ行い、他アプリに key を奪われて閉じた場合は何もしない、と条件を明記する。

## P1
- `ルート検索 > 2` — `NSMetadataQuery` は非同期かつ Spotlight インデックス依存で、結果が揃う前・インデックス再構築中は 0 件になりうる。また `com.apple.application-bundle` は `Foo.app/Contents/Library/LoginItems/Bar.app` のようなヘルパーも拾う / 起動直後の最初の ⌃L で候補が空になり、ヘルパーアプリが候補に混ざる / 起動時にクエリを走らせて結果を DB にキャッシュし、0 件ならディレクトリ走査にフォールバックする。パスに `.app/` が 2 回以上現れるものは除外する。
- `Phase 1 > 3` — DB のマイグレーション方針が無い。Phase 1 で DB を作り、Phase 3（履歴）・Phase 4（最近使った絵文字）でテーブルが増える / dev で貯めた DB を毎回消すことになり、`Phase 3 > 2` の「旧データ fixture を置いて起動する移行検証」自体が書けない / `PRAGMA user_version` によるバージョン付きマイグレーションの枠を Phase 1 で作り、各 Phase はそこにテーブル追加を足す形にする、と明記する。
- `Phase 2 > 7` — 検証フックの `--show` / `--query` / `--snapshot` が、既に常駐している dev プロセスに届かない。引数付きで起動しても別プロセスが立つだけ / 「常駐中のパネルを開いて撮る」という本来の用途で機能せず、Phase 2 以降の自動検証が回らない / 単一インスタンス化し、2 個目の起動は既存インスタンスへ Distributed Notification（または dev 専用 URL scheme）でコマンドを渡して自分は終了する、と決めておく。
- `クリップボード履歴 > 6` — 入力ソースの復元（ABC → 元）と、前面化 → ⌘V 合成の順序が決まっていない / IME の切り替え中に合成キーを投げると取りこぼし、「たまに貼り付かない」になる。今回 2 つの非同期処理を同時に足したので衝突しやすい / 「パネルを閉じる → 入力ソース復元 → 前面化の完了待ち → ペーストボードに書く → ⌘V」と順序を固定して書く。
- `ルート検索 > 1` — システム設定パネルの識別子を `.appex` からどう作るかが未検証。`x-apple.systempreferences:` に渡す `com.apple.Settings.*` が appex の bundle id とそのまま一致するとは限らず、macOS 26 で変わっている可能性もある / Phase 2 の終盤に「開かないパネルがある」と分かると収集ロジックを組み直すことになる / Phase 2 の最初に実機で全 appex を列挙して URL で開けるかを一度通しで確認し、開けないものだけ固定表で補う、という手順にする。
- `Phase 1 > 2` — dev の署名に Apple Development 証明書を指定している。この証明書は 1 年で失効し、再発行すると leaf 証明書の CN（`Apple Development: … (XXXXXXXXXX)`）が変わりうる / TCC はその CN で照合するため、更新のたびにアクセシビリティ許可が外れ、今回 P0 で潰したはずの問題がそのまま戻る。加えて notarize されないため Gatekeeper の挙動も常用版と揃わない / keyrc と同じ Developer ID Application で dev も署名する（`gen-signing-xcconfig.sh` の解決対象を Debug にも広げる）。

## P2
- `Phase 3 > 1` — 除外型の記述が「Concealed/Transient」のままで、前提側に足した `AutoGeneratedType` が抜けている / チェックリストだけ見て実装すると除外漏れになるので、文言を揃える。
- `クリップボード履歴 > 6` — ⌘Enter（コピーだけ）の後にパネルを閉じるかが書かれていない / 「閉じる」で統一しておくと、絵文字側（`絵文字ピッカー > 1`）の ⌘Enter とも挙動が揃う。
- `Raycast から引き継ぐ固定の挙動 > 3` — macOS 側の「書類ごとに入力ソースを自動で切り替える」が ON だと、OS の復元と mycast の復元が二重に走って戻り先が食い違うことがある / 実機の設定を一度確認し、ON なら復元処理の扱いをログに残しておく。

## Q

````

**対応**: P0（閉じる理由での場合分け）を反映: 元アプリへの復帰は Esc / ホットキー再押下 / 項目の実行のときだけ、他アプリのクリックで閉じたときは何もしない（前提と Phase 2 の両方）。P1 はすべて書き換えで反映: NSMetadataQuery の結果を DB キャッシュ＋0件ならディレクトリ走査、アプリ内ヘルパー除外／`PRAGMA user_version` のマイグレーション枠を Phase 1 に／単一インスタンス化し 2 個目の起動はコマンドを既存インスタンスへ渡す（1行のみ、設計は実装時）／貼り付けの順序を固定（閉じる→入力ソース復元→前面化待ち→書く→⌘V）／システム設定パネルは Phase 2 の最初に実機で全件開けるか確かめ、開けないものを固定表で補う／dev の署名を Developer ID Application に。P2: 除外型の文言を AutoGenerated まで揃える、⌘Enter 後はパネルを閉じる（絵文字も同じ）を反映。見送り: 「書類ごとに入力ソースを自動切替」の実機確認（調査の追加で、実装時に挙動がずれたら確認すれば足りる）。

## 3回目

````text
再読しました。前回の P0 1 件・P1 6 件・P2 2 件がいずれも本文とチェックリストの両方に入っていることを確認しました。見送り分は繰り返しません。残りは今回の書き換えで生じた噛み合わせの問題だけです。

## P0

## P1
- `クリップボード履歴 > 7` — Secure Event Input（ターミナルでのパスワード入力中、1Password の入力欄など）が有効なアプリでは、アクセシビリティ許可があっても合成した ⌘V が届かない / 縮退の条件が「許可が無いとき」だけなので、許可済みなのに無言で貼り付かないケースが残り、原因がわからない不具合として後から追うことになる / `IsSecureEventInputEnabled()` を貼り付け直前に見て、有効なら既にある「コピーだけに落として、その旨を表示する」経路に乗せる。
- `Phase 2 > 7` — `NSVisualEffectView` の背景ぼかしはウィンドウサーバ側の合成なので、プロセス内描画（`bitmapImageRepForCachingDisplay` 等）で撮ると背景が抜けた絵になる / パネルの外観を `NSVisualEffectView` に決めたことで、`--snapshot` は「レイアウトの確認用」にしかならないのに、見た目の確認にも使えるつもりで作ると画面収録許可が要る方式に戻すことになる / スナップショットはレイアウト検証用と割り切る旨を書き、撮影時だけ不透明な背景色にフォールバックする（見た目自体は `動作確認` で人間が見る）。
- `Phase 2 > 7` — 単一インスタンス化の条件が「引数付きで 2 個目を起動したら」に限定されている / 引数なしで 2 個目が起動した場合は 2 つ常駐し、後発の `RegisterEventHotKey` が失敗して毎回 NSAlert が出る。`mise run build → run` の開発ループは引数なし起動なので、ここが一番踏みやすい / 引数の有無にかかわらず単一インスタンスにし（既存がいれば起動側は終了）、`.mise.toml` の run / install で先に既存プロセスを終了させる。
- `クリップボード履歴 > 6` — 固定した順序の「パネルを閉じる」と、`UI・常駐 > 3` の「項目の実行で閉じたときは記憶したアプリへ戻す」が重なっており、前面化が 2 回走る記述になっている / 閉じる処理の副作用で先に前面化が始まると、「完了を待つ」対象がどちらか曖昧になり、待ち合わせが効かず貼り付けが取りこぼされる / 貼り付け経路では閉じる処理に復帰を含めず、前面化は明示的に 1 回だけ行う、と書き分ける。

## P2
- `UI・常駐 > 2` — 「閉じるときに記憶したアプリをアクティブ化して戻す」と無条件に書いたままで、直後の項目で条件が付く形になっている / 項目 2 の側にも「（条件は次項）」を添えるか、条件を項目 2 に寄せると読み違えが減る。
- `Phase 3 > 4` — チェックリストが「元アプリの前面化 → ⌘V 合成」のままで、前提側で固定した 5 段階の順序と粒度が揃っていない / 前提を参照する旨を一言足しておく。
- `絵文字ピッカー > 1` — 絵文字の Enter も同じ ⌘V 合成だが、順序の固定・Secure Input / 許可なしの縮退に触れていない / 貼り付けを Phase 3 の共通処理として括り、Phase 4 はそれを使う、と明記する。
- `動作確認 > 4` — Raycast がログイン項目から外れていることは確認するが、mycast がログイン項目に入って再起動後も常駐していることの確認が無い / `SMAppService` はユーザー承認待ちになることがあるので、再ログイン後に ⌃L が効くことを 1 項目として足す。

## Q

````

**対応**: P0 なしで収束。書き換えで済む P1/P2 を反映: Secure Event Input 有効時もコピーだけに縮退／`--snapshot` はレイアウト確認用と割り切り撮影時のみ不透明背景／単一インスタンスは引数の有無にかかわらず適用、run/install で既存プロセスを先に終了／貼り付け経路では閉じる処理に復帰を含めず前面化は 1 回だけ／UI 項目 2 に「戻す条件は次項」／Phase 3 の貼り付け項目を前提の順序参照＋縮退条件に／絵文字は履歴と共通の貼り付け処理を使う。見送り: 動作確認に「再ログイン後もログイン項目として常駐し ⌃L が効く」を足す（追加系のため終了報告へ）。
