review session: a4e572fd-f46c-4d68-b8c6-d908a6adaccd

## 1回目

````text
ビルドとテスト（`mise run build` / `test`）はこのセッションで実行許可が下りなかったため、静的レビューのみです。

## P0

## P1
- `Sources/Launcher/LauncherController.swift:execute(copyOnly:)` — システム操作を全部 `close(.launched)` で閉じているので `previousApp` に戻らない / Sleep と Lock Screen は同じセッションに帰ってくる操作なのに、復帰後の前面アプリが LSUIElement の mycast（ウィンドウなし）のまま＝どこかをクリックするまでキー入力が死ぬ / sleep・lockScreen は復帰する理由（`.copied` 相当、または `restorePreviousApp()` を呼ぶ新しい `CloseReason`）で閉じる。restart・shutDown・quitAllApps は今の `.launched` のまま（終了させる相手を前面化しても無意味）
- `Sources/Launcher/SystemActions.swift:sendToLoginWindow` — `[.noReply]` で送っているため TCC 拒否（errAEEventNotPermitted -1743）や送信失敗が `catch` に来ない / 拒否されても `system.sent` とだけ出て「Enter したのに何も起きない」になり、VERIFY.md の「常用版で `system.failed` が出ないこと」が原理的に失敗しない検証項目になっている / 送信前に `AEDeterminePermissionToAutomateTarget(target, typeWildCard, typeWildCard, true)` の OSStatus をログに出す（初回の許可ダイアログもここで確実に出る）、または権限確認だけ短いタイムアウトの `.waitForReply` で行う
- `Sources/Launcher/AppIndex.swift:AppIndex.commands` — システム項目を `commands` に足したので `index.all`（`commands + apps + panes`）の先頭側に並び、`Ranker.rank` の同点タイブレーク（`$0.order < $1.order`）がシステム操作有利になる / 例: `sl` は Slack が 897（`900 - 3`）、Sleep も 897 で同点 → 使用履歴の加点がまだ無いアプリだと Sleep が選択位置 0 に来て、確認なしの Sleep が Enter 一発で走る / `all` を `apps + panes + commands` にする（または `sys:` 項目に小さな減点を入れる）＋ アプリ相当の `RankInput` と競合させるテストを足す
- `Sources/Launcher/LauncherController.swift:pressKey` — `--key enter` の dry run でも `execute` 内の `model.usage.record(item.id)` が走る / 検証フックが本物の frecency を書き換え、以後のルート検索の並びが検証のせいで変わる（CLAUDE.md の「検証で `NSPasteboard` に書かない」と同じ筋の副作用）/ `systemDryRun` のときは usage の記録も飛ばす（記録を `case .system` の実行側へ移すか、`record` をフラグでガードする）

## P2
- `Sources/Launcher/LauncherView.swift:RootRow` — `armed` の判定を `"sys:\($0.rawValue)"` と id を組み直して比較している / `AppIndex` の id 規則を 2 か所に複製しており、id を変えた瞬間に確認表示だけが黙って出なくなる / `if case .system(let a) = item.kind, a == model.armedAction` で kind を見る
- `Sources/Launcher/SystemActions.swift:lockScreen` — `CGEvent` フォールバックはアクセシビリティ未許可だと何も起きず、ログには `fallback=ctrl_cmd_q` と成功したように残る / 私用 API が消えた将来に原因が分からなくなる / `Paster` と同じく `AXIsProcessTrusted()` を見て未許可をログに出す（プロンプトの出し方も `Paster` に倣う）
- `Sources/Launcher/SystemActions.swift:quitAllApps` — `terminate()` の戻り値を捨てている / `system.quit_all count=` が「依頼した数」で、送信に失敗したアプリが見えない / 失敗したものだけ名前をログに出す
- `Sources/Launcher/LauncherController.swift:handleKey` — システム行で ⌘Enter が Enter と同じ（arm→実行）になる / 他の画面と計算カードでは ⌘Enter は「コピーして元のアプリへ戻る」なので、コピーのつもりの ⌘Enter で Shut Down を確定できてしまう / システム行では `.command` 付きを無視する（フッターの `⌘↵` も出ない前提に合わせる）
- `Sources/Core/SystemCommand.swift:QuitAllPolicy.shouldQuit` — `isSelf` は実際には効かない（mycast は LSUIElement ＝ `.accessory` なので `isRegular` で既に落ちる。併存する dev / 常用版も同様）/ 死んだ条件とそれを守るテストが残る / pid 除外を残すなら「将来 LSUIElement を外したときの保険」だとコメントに書く、でなければ `isRegular` だけにする
- `Sources/Info.plist:NSAppleEventsUsageDescription` — 文面が「再起動・シャットダウン・スリープ」限定 / Close All Apps の `terminate()` も各アプリへの quit Apple Event なので、そこでダイアログが出たときに説明と食い違う / 「アプリの終了」も含む文面にする（`project.yml` の `info.properties` が源なので両方）
- `Tests/CoreTests.swift:SystemCommandTests.testSearchFindsSystemActions` — システム操作同士でしかランキングを見ていない / P1 のアプリとの同点衝突を固定できていない / `Slack` 相当の `RankInput` を混ぜて「アプリが勝つ」ことを検証する

## Q
- `Sources/Core/SystemCommand.swift:SystemAction.keys` — 1 文字クエリでシステム操作を候補に出すか（タイブレークを直すだけで済ませるか、`sys:` は 2〜3 文字以上でのみ出すか、Sleep にも確認を付けるか）/ 決めないと `s` / `sl` のような打ち始めの 1 Enter でスリープに入る事故が残る（CLAUDE.md が決めているのは「Sleep は確認しない」までで、候補に出す閾値は未決）
- `Sources/Launcher/LauncherController.swift:close(_:)` — 「元のアプリへ戻すのは Esc / ホットキー再押下 / ⌘Enter のときだけ」という CLAUDE.md の不変条件に、Sleep・Lock Screen の例外を足して良いか / 決めないと P1 の 1 件目を直せない（直すなら CLAUDE.md の当該行も併せて更新が必要）

````

**対応**:
- P1 execute(copyOnly:) Sleep/Lock の復帰: CloseReason.suspended を足し、sleep/lockScreen はそれで閉じて previousApp に戻す。CLAUDE.md の「元のアプリへ戻す」行に Sleep・Lock Screen を追記
- P1 sendToLoginWindow: 送信前に AEDeterminePermissionToAutomateTarget(askUserIfNeeded: true) を呼び、noErr 以外は system.failed permission=<status> で中止
- P1 AppIndex.commands のタイブレーク: システム操作を systemCommands に分けて all の末尾（apps + panes の後）へ移動
- P1 pressKey の dry run で usage.record: systemDryRun のときは記録しない
- P2 RootRow の armed 判定: id の組み直しをやめ、kind の .system(a) と armedAction を比較
- P2 quitAllApps: terminate() の戻り値を見て failed= をログに出す
- P2 ⌘Enter: システム行では copyOnly のとき何もしない
- P2 QuitAllPolicy.isSelf: LSUIElement を外したときの保険とコメントで明記して残す
- P2 NSAppleEventsUsageDescription: 「アプリの終了」を含む文面に変更（project.yml → Info.plist 再生成）
- 見送り P2 lockScreen の AXIsProcessTrusted チェック: 仕組みの追加なのでループ中は見送り、終了報告に回す
- 見送り P2 Slack 相当とのランキング競合テスト: 並び順は AppIndex（テスト対象外の Sources/Launcher）側で決まるため Core のテストでは固定できない。テスト追加は終了報告に回す
- Q SystemAction.keys（1 文字クエリの閾値）: 決定=閾値は設けない。タイブレークを末尾に回したことで同点ならアプリが勝つので、打ち始めで Sleep が先頭に来る事故は解消 / 根拠: Ranker.rank の order タイブレーク
- Q close(_:) の不変条件の例外: 決定=Sleep・Lock Screen は戻す。CLAUDE.md の不変条件の理由は「クリックしたアプリが裏に回る」ことで、スリープ・ロックは該当しない / 根拠: CLAUDE.md 罠の節

## 2回目

````text
ビルドとテスト（`mise run build` / `test`、`swiftc -typecheck` も）はこのセッションで実行許可が下りなかったため、静的レビューのみです。plan は `docs/plans/` にこの機能ぶんが無いので、CLAUDE.md の固定挙動と前回の決定を基準にしました。

## P0
- `Sources/Launcher/SystemActions.swift:sendToLoginWindow` — `AEDeterminePermissionToAutomateTarget(desc, typeWildCard, typeWildCard, true)` が `typeWildCard` を素で渡している / Carbon の無名 enum 定数は Swift では `Int` で入るため、`AEEventClass`(= `UInt32`)・`AEEventID` の引数と型が合わずコンパイルが通らない。同じファイルで `AEEventClass(kCoreEventClass)` / `AEReturnID(kAutoGenerateReturnID)`、`Sources/App/HotKeys.swift` で `EventParamType(typeEventHotKeyID)` と包んでいるのと同じ理由（`noErr` は `OSStatus` なので `status == noErr` は既存コードどおりで問題ない）/ `AEDeterminePermissionToAutomateTarget(desc, AEEventClass(typeWildCard), AEEventID(typeWildCard), true)` にする。ビルドで確認する（この指摘だけは実機ビルド未確認）

## P1
- `Sources/Launcher/LauncherController.swift:execute(copyOnly:)` — dry run でも `close(.suspended)` を通るので、`--key enter` で Sleep / Lock Screen の行を撃つと `restorePreviousApp()` が走り `previousApp.activate()` が発火する / 検証フックの不変条件（`--show` はフォーカスを奪わない、他アプリに作用する Enter は撃たない）が崩れる。`--show` から `--key enter` までの間にユーザーがアプリを切り替えていると、検証が前のアプリを前面に引き戻す（前回直した `usage.record` と同じ筋の副作用）/ `systemDryRun` のときは復帰しない理由で閉じる（`close(systemDryRun ? .launched : (action.returnsToPreviousApp ? .suspended : .launched))` 相当）

## P2
- `Sources/Launcher/LauncherController.swift:execute(copyOnly:)` — `action == .sleep || action == .lockScreen` という分類をコントローラに直書きしている / 「すぐ戻れる操作」という同じ軸の判定が `SystemAction.needsConfirmation`（Core・テスト可能）と 2 か所に分かれ、操作を足したときに片方だけ直す / `SystemAction` に `var returnsToPreviousApp: Bool` を足して `needsConfirmation` の隣に置き、テストも 1 行足す
- `Sources/Launcher/SystemActions.swift:sendToLoginWindow` — `if let desc = target.aeDesc` が外れたときは許可チェックを黙って飛ばして送信に進む / 起きたときに「権限を見ずに送った」ことがログから分からない / else で `system.failed \(action) no_aedesc` 相当を出す（送信自体は続けて良い）
- `Sources/Launcher/LauncherView.swift:RootListView` — `armed:` に即時実行クロージャ（`{ if case ... }()`）を埋めていて ForEach の 1 行が読みにくい / body 内の他の行（`RootRow`・`CalcCard`）が素直な式だけなのと不揃いで、条件が増えると壊れやすい / `RootListView` のメソッド（`private func isArmed(_ item: RootItem) -> Bool`）か `LauncherModel.isArmed(_:)` に出す

## Q

````

**対応**:
- P0 sendToLoginWindow の typeWildCard: 反映せず。前回の対応後に mise run build が BUILD SUCCEEDED（typeWildCard は OSType として import されており AEEventClass/AEEventID と同型）。今回の修正後も BUILD SUCCEEDED を再確認
- P1 dry run で close(.suspended): systemDryRun のときは .launched で閉じ、前面化しない
- P2 分類の直書き: SystemAction.returnsToPreviousApp を Core に足し、コントローラはそれを見る。テストに 1 行追加
- P2 aeDesc が nil のとき: system.permission_unchecked <action> no_aedesc をログに出して送信は続行
- P2 RootListView の即時実行クロージャ: RootListView.isArmed(_:) に出した

## 3回目

````text
前回の P0（`typeWildCard`）は私の誤りでした。`typeWildCard` は `OSType` として import されるので `AEEventClass` / `AEEventID` と同型で、ビルドが通るのが正しい挙動です。今回は差分を読み直して再レビューしました（`mise run build` / `test` はこのセッションでも実行許可が下りないため、BUILD SUCCEEDED の報告を前提にしています）。

## P0

## P1
- `Sources/Launcher/SystemActions.swift:sendToLoginWindow` — 許可が下りなかったとき（`system.failed … permission=-1743` など）ログだけで、ユーザーには何も出ない / TCC の拒否は sticky なので、Restart を Enter 2 回押しても以後ずっと無反応のまま原因が分からない。同じ筋の「アクセシビリティ許可が無い」ケースは `Toast.show` で知らせる作りになっており（`Sources/Launcher/Paster.swift:Toast` はパネルを閉じた後に出す独立パネル、VERIFY.md の手動確認にも項目がある）、システム操作だけ黙って落ちるのが不揃い / `guard status == noErr` の中と `catch` で `Toast.show("再起動を依頼できませんでした（システム設定 → プライバシーとセキュリティ → オートメーション）")` 相当を出す。VERIFY.md の手動確認にも 1 行足す

## P2
- `VERIFY.md:ログ` — 今回足した `system.permission_unchecked` がイベント名の一覧に無い / 「先頭の語がイベント名」として列挙している節なので、ログを見たときにどこから出たか辿れない（他の `system.*` は載っている）/ `system.dry_run` の隣に足す
- `Sources/Core/SystemCommand.swift:SystemAction.keys` — `quitAllApps` だけローマ字キーが無い（他の 4 つは `suripu` / `rokku` / `saikidou` / `shattodaun` を持つ）/ ルートの検索欄は ABC 固定なので、日本語名（`すべてのアプリを終了`）はキーボードから引けず、実質 `close` / `quit` の英語だけになる / `subete` など日本語側のローマ字キーを 1 つ足すか、日本語キーに頼らない方針なら他の 4 つからローマ字を外して揃える

## Q

````

**対応**: 収束。P2 VERIFY.md のイベント名一覧に system.permission_unchecked を追記。P2 quitAllApps にローマ字キー subete を追加。見送り P1 許可拒否時の Toast: 仕組みの追加なのでループ中は見送り、終了報告に回す

